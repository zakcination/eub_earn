{
  "name": "eub-earn",
  "version": "1.0.0",
  "private": true,
  "description": "Mini app to estimate overtime earnings from reported overtime hours, and reverse-calculate hours from a payment amount.",
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  }
}
